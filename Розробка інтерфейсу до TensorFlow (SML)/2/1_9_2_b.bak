#lang racket
(define (min x y)
  (if (and (number? x) (number? y))
      (if (< x y) x
          (if (= x y)
              "Невозможно найти минимальное число, так как они равны!"
              y))
      "Некорректные данные!"
      )
  )